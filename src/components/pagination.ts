// Composant pagination HTML
// TODO: Pagination pour listes longues (patients, consultations...)
export function paginationHTML(page: number, total: number, baseUrl: string): string {
  return `<!-- TODO: Composant pagination -->`
}
