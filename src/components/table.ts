// Composant tableau HTML réutilisable
// TODO: Tableau avec tri, recherche, pagination
export function tableHTML(colonnes: string[], lignes: any[][]): string {
  return `<!-- TODO: Composant tableau -->`
}
