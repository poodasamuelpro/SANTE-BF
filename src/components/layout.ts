// Layout HTML réutilisable (header + nav + footer)
// TODO: Header avec nom utilisateur, rôle, structure, bouton déconnexion
export function layout(titre: string, contenu: string, profil: any): string {
  return `<!-- TODO: Layout principal SantéBF -->`
}
