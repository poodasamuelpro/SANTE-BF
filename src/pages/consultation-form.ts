// Formulaire de nouvelle consultation
// TODO: Champs : motif, anamnèse, examen clinique, diagnostic, conduite à tenir
export function consultationFormPage(patient: any, erreur?: string): string {
  return `<!-- TODO: Formulaire consultation -->`
}
