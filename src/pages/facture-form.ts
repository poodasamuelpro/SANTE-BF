// Formulaire de facturation (caissier)
// TODO: Sélectionner actes du catalogue, calculer total, mode paiement
export function factureFormPage(patient: any, actes: any[]): string {
  return `<!-- TODO: Formulaire facture -->`
}
