// HTML de la fiche patient complète (vue médecin)
// TODO: Toutes les infos patient, constantes, historique consultations
export function fichePatientPage(patient: any, consultations: any[]): string {
  return `<!-- TODO: Fiche patient complète -->`
}
