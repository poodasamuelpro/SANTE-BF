// Page de réinitialisation de mot de passe
// TODO: Formulaire email pour reset + page de confirmation
export function resetPasswordPage(erreur?: string): string {
  return `<!-- TODO: Page reset password -->`
}
export function resetConfirmPage(erreur?: string): string {
  return `<!-- TODO: Page confirmation nouveau mot de passe -->`
}
